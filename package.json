{
  "name": "works-in-korea",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Works in Korea? — measurement engine",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "seed": "tsx src/seed.ts",
    "probe": "tsx src/run-probes.ts",
    "validate": "tsx src/validate.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run validate",
    "hints": "tsx src/hints-todo.ts",
    "check-urls": "tsx src/check-urls.ts",
    "discover": "tsx src/discover-support.ts",
    "inspect": "tsx src/inspect-page.ts",
    "ingest": "tsx src/ingest-report.ts",
    "find-hints": "tsx src/discover-hints.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
