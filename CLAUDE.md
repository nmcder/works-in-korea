# Works in Korea? — 프로젝트 인수인계 문서

> 이 문서는 **다른 Claude Code 세션에서 이어받기 위해** 작성됐습니다.
> 이 폴더에서 처음 세션을 시작했다면 이 문서를 끝까지 읽고 `docs/` 를 확인한 뒤 작업하세요.
> 최초 작성: 2026-08-15 (이전 세션에서 시장조사 → 아이디어 선정 → 설계 확정 완료)

---

## 1. 이 프로젝트가 뭔가

**Works in Korea?** — 한국 온라인 서비스(웹·앱)가 **외국인에게 실제로 작동하는지**를 매일 자동으로 측정해서, **측정 시각·측정 방법과 함께** 공개하는 열린 데이터베이스.

예: "쿠팡은 해외 발급 카드로 결제되나?", "이 사이트 가입에 한국 휴대폰 본인인증이 강제인가?", "해외 IP에서 접속되나?", "영어 UI가 실제로 있나?"

**경쟁 우위는 커버리지가 아니라 `신선도 + 구조화 + 출처 추적성`입니다.**
기존 정보원(블로그 글)은 분기 1회 수동 갱신이고 측정 시각이 없어서, 정보가 낡았는지 알 수 없습니다.

---

## 2. 운영자 프로필 — 반드시 숙지

| 항목 | 내용 |
|---|---|
| 신분 | **고등학교 3학년**, 1인 개발자 |
| **코딩 능력** | **직접 코딩 못 함. Claude가 개발을 전담하고, 운영자는 의사결정·검수·홍보를 담당** |
| 가용 시간 | 주 10시간 이상 |
| 예산 | 월 10만원 (현재 설계상 인프라 0원이라 대부분 여유) |
| 사업자등록 | **없음.** 1년 내 등록 예정 |
| 목적 | 포트폴리오 + 명예 + 이용자 봉사. 추후 상업적 확장 가능하도록 |
| GitHub | https://github.com/nmcder (사용 가능) |
| 소통 언어 | 한국어 |

**중요:** 운영자에게 코드를 읽고 판단하라고 요구하지 마세요. 선택지를 제시할 때는 기술 용어가 아니라 **결과와 트레이드오프**로 설명하세요.

---

## 3. 절대 규칙 — 어기지 말 것

이건 전부 이전 세션에서 근거를 갖고 결정된 것입니다. 배경은 `docs/03-decisions.md` 참고.

1. **수익 창구를 만들지 말 것.** 광고(애드센스), 제휴 링크, 후원 버튼, 결제 전부 금지.
   → 사업자등록이 없는데 반복적 수입이 생기면 사업소득으로 사업자등록 의무가 발생하고,
     개인 신분으로는 세법상 기부 수령도 불가(증여세 대상)합니다. 사업자등록 전까지 수익 0개 유지.

2. **로그인·회원가입·개인정보 수집을 만들지 말 것.**
   → 개정 개인정보보호법이 2026-09-11 시행. 72시간 내 유출 통지 의무, 과징금 상한 매출 10%.
     1인 학생 운영자에게 회원 DB는 순수 부채입니다. 제보는 GitHub Issue Form으로 받습니다.

3. **GitHub 레포는 프라이빗.**
   → 운영자가 복제 우려를 제기했고, 검증 결과 퍼블릭이 필수가 아님을 확인했습니다.
     퍼블릭→프라이빗은 되돌릴 수 없으므로 자산이 쌓인 뒤 재결정합니다.
     단, **산출 데이터와 웹사이트는 공개**합니다 (하이브리드).

4. **크롤링 윤리를 지킬 것.**
   - robots.txt 준수, 공개 페이지 GET만
   - **자동 결제 시도 절대 금지** (결제 관련은 전부 커뮤니티 제보로만)
   - **자동 로그인·계정 생성 시도 금지**
   - 저빈도 (기본 하루 1회)
   - User-Agent에 프로젝트 URL과 연락처 명시

5. **모든 측정 데이터에 메타 3종을 붙일 것:** `측정시각 / 측정방법 / 신뢰도(자동·제보·미확인)`
   → 모르면 "모름"으로 표시하는 게 이 제품의 핵심 자산입니다. 추측으로 채우지 마세요.

6. **평가하지 말고 사실만 기술할 것.** "이 사이트는 외국인에게 불친절함" ❌ / "2026-08-15 측정 시점에 가입 단계에서 한국 휴대폰 본인인증이 요구됨" ⭕

---

## 4. 아키텍처 — 서버 0대, 비용 0원

```
GitHub Actions (프라이빗 레포) + Playwright
  │  ※ Actions 러너가 해외에 있다는 점이 버그가 아니라 기능.
  │    기본 실행 환경이 곧 "외국인 방문자의 시점"이다.
  │  ※ 프라이빗 Actions 무료 한도: Free 2,000분/월, Pro 3,000분/월.
  │    100개 서비스 일 1회 ≈ 월 600분 추정 → Free 플랜만으로도 3배 여유.
  │    학생팩(고등학생도 대상)이 승인되면 Pro + 도메인 무료가 보너스로 붙지만,
  │    학생팩은 전제 조건이 아니다. 승인 여부와 무관하게 진행할 것.
  ▼
측정 결과를 레포에 JSON으로 커밋
  │  ※ 데이터가 git에 들어가므로 "언제 무엇이 바뀌었는지"가 git log에 공짜로 축적된다.
  │    이게 시간이 갈수록 커지는 해자다. 복제자는 스냅샷만 얻고 0일차부터 시작한다.
  ▼
Next.js 정적 빌드 → Vercel Hobby (비상업 전용 티어 = 현재 조건에 정확히 부합)
  +
공개 JSON API (정적 파일 서빙 → 서버 불필요)
  +
제보 = GitHub Issue Form (백엔드·DB·스팸필터를 GitHub에 위임, 개인정보 수집 0)
```

**비용:** 인프라 0원. 도메인은 학생팩 승인 시 Namecheap `.me` 1년 무료, 아니면 연 약 2만원.
LLM API(제보 정규화용)만 월 3~5만원 예상. 어느 쪽이든 월 예산 10만원 안에서 여유가 큼.

---

## 5. 현재 상태

- [x] 시장조사 완료 (`docs/01-market-research.md`)
- [x] 아이디어 선정·검증 완료
- [x] 제품 정의·아키텍처 확정 (`docs/02-product-spec.md`)
- [x] 주요 의사결정 기록 (`docs/03-decisions.md`)
- [x] **1주차 완료 (2026-08-15)** — 데이터 스키마 + 자동 프로브 6종 + Actions 크론
- [x] **2주차 완료 (2026-08-15)** — 공개 사이트 `site/` (홈·상세·`/changes`·`/method`·공개 API)
- [x] **배포 완료 (2026-08-15)** — **https://works-in-korea.vercel.app** (Vercel Hobby, 무료)
- [ ] **← 여기부터 시작.** 3주차: 제보 레이어 (GitHub Issue Form → 정규화 → PR)

**공개 주소**

| | |
|---|---|
| 사이트 | https://works-in-korea.vercel.app |
| 공개 API | `/api/v1/services.json` · `/api/v1/services/<id>.json` · `/api/v1/changes.json` · `/api/v1/meta.json` |
| 갱신 | 크론이 `data/` 커밋 → 푸시 → Vercel 자동 재배포. **사람 개입 없음** |

배포 설정은 레포 최상위 `vercel.json` 에 있다. Vercel UI에서는 아무것도 설정하지 않는다 (D-13).

로드맵은 `docs/04-roadmap.md`. 엔진 사용법과 사이트 실행법은 `README.md`.

**사이트를 눈으로 보려면**
```bash
cd site && npm install && npm run dev
```

**2주차에서 남긴 것 (3주차 전에 처리)**
1. **제보 창구가 아직 없다.** 사이트 곳곳에서 "제보로만 채울 수 있다"고 말하고 있는데
   정작 넣을 곳이 이메일뿐이다. 3주차의 Issue Form 이 나오면
   `site/lib/site-config.ts` 의 `reportUrl` 만 채우면 UI가 붙는다.
2. **마지막 측정이 한국 IP에서 실행됐다** (`data/runs/latest.json` 의 `vantage_point.country = kr`).
   해외에서만 막히는 것은 그 실행에서 정상으로 보였을 것이다. `/method` 페이지가 이 사실을
   자동으로 경고하도록 해 뒀다 — 크론(해외 러너)이 한 번 돌면 경고는 저절로 사라진다.
3. **SPA 렌더링 흔들림 안정화** (D-10 숙제) 는 아직 안 했다. `/changes` 에 가짜 변경이
   잡힐 여지가 남아 있어, 페이지에 "측정값이 바뀐 것이지 회사가 바꾼 증명이 아니다"라고 명시해 뒀다.

**1주차 실측 요약** (전체 실행 8분 18초 · 월 250분 · Free 2,000분의 12%)

| 시그널 | 측정됨 / 106 | 막는 것 |
|---|---|---|
| `overseas_access` | 78 | robots.txt·봇차단 28건 |
| `i18n_ui` | 78 | 위와 동일 |
| `payment_gate` | 78 (PG 탐지는 11) | 결제 페이지가 로그인 뒤 |
| `signup_phone_auth` | 20 | 가입 URL 미상 → `hints.signup_url` |
| `app_availability` | 20 | 앱 ID 미상 → `hints.*_app_id` |
| `support_en` | 1 | `ANTHROPIC_API_KEY` 미설정 |

**1주차에 확정된 제약 2가지 — 2주차 설계에 반영할 것**
1. **시드의 26%(28건)는 robots.txt·봇차단으로 자동 측정이 영구 불가**하다 (D-9).
   네이버·카카오·인터파크·무신사·멜론 등 주요 서비스가 포함된다.
   → 3주차 제보 레이어는 "보완"이 아니라 이들의 **유일한 데이터원**이다. 우선순위를 높일 것.
2. **측정 불가 항목은 추측으로 채우지 않고 범위를 좁혔다** (D-8).
   앱 국가별 출시 여부는 제거, 봇차단/지역차단 구분 불가 시 `unknown`.

---

## 6. 세션 시작 시 운영자에게 확인할 것

1. GitHub Student Developer Pack 신청 상태 (**대기 중이어도 진행할 것.** Free 2,000분으로 충분)
2. 프라이빗 레포를 만들었는지 (없으면 Claude가 `gh` CLI로 생성 제안)
3. 도메인을 정했는지 (미정이면 스키마 작업 먼저 진행해도 무방)

---

## 7. 문서 안내

| 파일 | 내용 |
|---|---|
| `docs/01-market-research.md` | 왜 이 아이템인가. 시장 근거 수치와 출처 전체 |
| `docs/02-product-spec.md` | 무엇을 측정하는가. 데이터 모델, 프로브 명세 |
| `docs/03-decisions.md` | 왜 이렇게 정했는가. 결정 로그 (재논의 방지) |
| `docs/04-roadmap.md` | 4주 MVP 계획과 역할 분담 |

**사이트를 건드리기 전에 반드시 읽을 것:** `docs/03-decisions.md` 의 **D-12 (화면 규칙)**.
"측정 안 됨"을 어떻게 그릴지, 색이 무엇을 뜻하는지, 기본 정렬을 왜 그렇게 했는지가 근거와 함께 있다.
이 규칙을 모르고 고치면 이 제품의 핵심 자산(모른다고 말하는 것)이 조용히 사라진다.
