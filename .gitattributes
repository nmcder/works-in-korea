# 로컬은 Windows, 크론은 Linux(Actions)에서 같은 파일을 쓴다.
# 줄바꿈을 LF로 고정하지 않으면 매일 전체 파일이 "변경됨"으로 잡혀서
# git diff가 무의미해진다. 이 프로젝트의 해자가 git 이력이므로 중요하다.
* text=auto eol=lf

# 측정 산출물은 항상 LF
*.json text eol=lf
*.ts   text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
